package com.ironhold.config.dto;

/** One enemy type line inside a mixed wave entry. */
public final class WaveSpawnEntryDto {
    public String enemyId = "grunt";
    public int count = 5;
    public float spawnIntervalSec = 1.0f;
}
