package com.ironhold.game.model;

/** Visual and combat behaviour for {@link ActiveProjectile}. */
public enum ProjectileKind {
    /** Fast energy beam (Dart tower). */
    BEAM,
    /** Slow arcing shell with splash on landing (Mortar tower). */
    MORTAR_SHELL
}
